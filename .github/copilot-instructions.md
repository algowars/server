# Copilot Instructions

## Project Guidelines
- ResultId in submission_results should only be populated after submission report execution is complete. At all other stages (initialization, polling), it should remain null.